{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["all"],
  "functions": {
    "api/**/*": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
} 